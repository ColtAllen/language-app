[project]
name = "language-app"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "streamlit",
    "langchain",
    "langchain-openai",
    "openai",
    "python-dotenv",
    "pillow",
]
